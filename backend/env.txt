# ============================================================
#  RAG Chatbot — API Keys & Configuration
#
#  HOW TO USE:
#  1. Fill in your OpenAI API key below (the only required field).
#  2. Rename this file from  env.txt  to  .env
#     (or keep editing this file and copy-paste into .env)
# ============================================================

# --- REQUIRED: Your OpenAI API key ---
# Get one at: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# --- LLM Model used to generate answers ---
LLM_MODEL=gpt-4o-mini

# --- Embedding model used to index documents ---
EMBEDDING_MODEL=text-embedding-3-small

# --- How many document chunks to pull per query (1-10) ---
RETRIEVAL_TOP_K=4

# --- Text chunking: characters per chunk ---
CHUNK_SIZE=1000

# --- Overlap between consecutive chunks (characters) ---
CHUNK_OVERLAP=200

# --- Folder where ChromaDB saves its vector index ---
CHROMA_PERSIST_DIR=./chroma_db

# --- FastAPI server host & port ---
HOST=0.0.0.0
PORT=8000

# --- Allowed frontend origins (comma-separated) ---
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:5500,http://localhost:5500
